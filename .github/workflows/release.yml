name: Release Obsidian plugin

on:
  push:
    tags:
      - '*'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18.x'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Build plugin
        env:
          NS_NSDAV_ENDPOINT: ${{ vars.NS_NSDAV_ENDPOINT || 'https://dav.jianguoyun.com/nsdav' }}
          NS_DAV_ENDPOINT: ${{ vars.NS_DAV_ENDPOINT || 'https://dav.jianguoyun.com/dav' }}
          NODE_ENV: production
        run: |
          pnpm install
          pnpm run build

      - name: Package plugin
        run: |
          tag="${GITHUB_REF#refs/tags/}"
          mkdir ${{ github.event.repository.name }}
          cp main.js manifest.json styles.css ${{ github.event.repository.name }}
          zip -r ${{ github.event.repository.name }}-${tag}.zip ${{ github.event.repository.name }}
          tar -czf ${{ github.event.repository.name }}-${tag}.tar.gz ${{ github.event.repository.name }}

      - name: Create release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          tag="${GITHUB_REF#refs/tags/}"

          gh release create "$tag" \
            --title="$tag" \
            --latest \
            main.js manifest.json styles.css \
            ${{ github.event.repository.name }}-${tag}.zip \
            ${{ github.event.repository.name }}-${tag}.tar.gz
